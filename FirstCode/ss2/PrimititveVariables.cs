using System;
using System.Collections.Generic;
using System.Text;

namespace FirstCode.ss2
{
    internal class PrimititveVariables
    {
        private static void Main3(string[] args)
        {
            ////SIGNED INTEGER TYPES
            ///*Have a + or - sign. Use them when numbers can drop below zero (like temperature or bank balances).
            // */
            //sbyte sb = -120; //8 bits, -2^7 to 2^7 - 1
            //short s = -32000; //16 bits, -2^15 to 2^15 - 1
            //int i = -2000000000; //32 bits, -2^31 to 2^31 - 1
            //long l = -9000000000000000000; //64 bits, -2^63 to 2^63 - 1
            //Console.WriteLine(sb);
            //Console.WriteLine(s);
            //Console.WriteLine(i);
            //Console.WriteLine(l);

            ////UNSIGNED INTEGER TYPES
            ///*Do not have a + or - sign. Use them when numbers will never drop below zero (like age, number of people, or number of cars).
            // */
            //byte b = 255; //8 bits, 0 to 2^8 - 1
            //ushort us = 65000; //16 bits, 0 to 2^16 - 1
            //uint ui = 4000000000; //32 bits, 0 to 2^32 - 1
            //ulong ul = 18000000000000000000; //64 bits, 0 to 2^64 - 1
            //Console.WriteLine(b);
            //Console.WriteLine(us);
            //Console.WriteLine(ui);
            //Console.WriteLine(ul);

            ////Because unsigned types do not waste memory keeping track of negative numbers, they can fit positive numbers twice as large in the exat same space.

            ////FLOATING POINT TYPES
            ///*Use them when you need to store numbers with decimal points. They can store very large or very small numbers, but they are not exact. Use them when you need to store numbers that are not exact, like measurements or scientific data.
            //Suffix : -f (float), none or -d (double), -m (decimal)
            //*/
            //float f = 12.7f;
            //double d = 23.67;
            //decimal dm = 99.1079m;
            //Console.WriteLine(f);
            //Console.WriteLine(d);
            //Console.WriteLine(dm);

            ////OTHER VARIABLE TYPES
            ///*bool - true or false
            // * char - a single character (uses single quotes ' ')
            // * string - text or sequnce of characters (uses double quotes " ")
            // */
            //char c = 'a';
            //bool bo = true;
            //string str = "Hello World!";
            //Console.WriteLine(bo);
            //Console.WriteLine(!bo);
            //Console.WriteLine(str);
            //Console.WriteLine(c);




        }
    }
}
